{
  "name": "dishant-gupta-portfolio-monorepo",
  "version": "2.0.0",
  "private": true,
  "description": "Modern portfolio website with Next.js frontend and Flask API backend",
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:api": "cd api && python index.py",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start",
    "install:all": "npm install && cd frontend && npm install && cd ../api && pip install -r requirements.txt",
    "seed": "cd api && python seed.py",
    "lint": "cd frontend && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Dishant Gupta",
  "license": "MIT"
}
